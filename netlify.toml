[build]
  publish = "."
  functions = "netlify/functions"

# Route /c/:companyId → index.html (SPA routing)
[[redirects]]
  from = "/c/*"
  to = "/index.html"
  status = 200

# Fallback for any other unknown routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
